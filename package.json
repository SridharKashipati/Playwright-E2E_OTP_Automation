{
  "name": "playwright-e2e-email-otp",
  "version": "1.0.0",
  "description": "Playwright E2E automation for Email OTP flow",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "test:headed": "playwright test --headed",
    "test:report": "npx playwright show-report",
    "test:ci": "npx playwright test --reporter=html",
    "cleanup": "rm -rf test-results playwright-report"
  },
  "author": "Sridhar Kashipati",
  "dependencies": {
    "axios": "^1.11.0",
    "imap-simple": "^6.0.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "@types/node": "^24.3.0",
    "dotenv": "^16.6.1",
    "imap-simple": "^6.0.0"
  }
}
